{
  "name": "land-layout-optimizer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && pnpm dev",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --port 8000",
    "dev": "concurrently \"pnpm dev:frontend\" \"pnpm dev:backend\"",
    "build": "cd frontend && pnpm build"
  }
}
